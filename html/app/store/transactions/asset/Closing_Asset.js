
Ext.define('App.store.transactions.asset.Closing_Asset', {
	requires: [ 'App.model.transactions.asset.Closing_Asset' ],
    model: 'App.model.transactions.asset.Closing_Asset',
    extend: 'Ext.data.Store'
});